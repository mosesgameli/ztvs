name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }}/${{ matrix.arch }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux
          - os: linux
            arch: amd64
          - os: linux
            arch: arm64
          # macOS
          - os: darwin
            arch: amd64
          - os: darwin
            arch: arm64
          # Windows
          - os: windows
            arch: amd64
          - os: windows
            arch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Set binary extension
        id: ext
        run: |
          if [ "${{ matrix.os }}" = "windows" ]; then
            echo "bin_ext=.exe" >> "$GITHUB_OUTPUT"
            echo "archive_ext=zip"  >> "$GITHUB_OUTPUT"
          else
            echo "bin_ext="         >> "$GITHUB_OUTPUT"
            echo "archive_ext=tar.gz" >> "$GITHUB_OUTPUT"
          fi

      - name: Build project
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: "0"
        run: |
          go build -ldflags="-s -w -X github.com/mosesgameli/ztvs/internal/cli.Version=${{ github.ref_name }}" -o zt${{ steps.ext.outputs.bin_ext }} ./cmd/zt

      - name: Prepare dist directory
        run: |
          mkdir -p dist
          cp zt${{ steps.ext.outputs.bin_ext }} dist/

      - name: Package archive (tar.gz)
        if: matrix.os != 'windows'
        run: |
          ARCHIVE="ztvs-${{ github.ref_name }}-${{ matrix.os }}-${{ matrix.arch }}.tar.gz"
          tar -czf "$ARCHIVE" -C dist .
          echo "ASSET=$ARCHIVE" >> "$GITHUB_ENV"

      - name: Package archive (zip)
        if: matrix.os == 'windows'
        run: |
          ARCHIVE="ztvs-${{ github.ref_name }}-${{ matrix.os }}-${{ matrix.arch }}.zip"
          cd dist && zip -r "../$ARCHIVE" . && cd ..
          echo "ASSET=$ARCHIVE" >> "$GITHUB_ENV"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ztvs-${{ matrix.os }}-${{ matrix.arch }}
          path: ${{ env.ASSET }}
          if-no-files-found: error

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v8
        with:
          path: release-assets
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
